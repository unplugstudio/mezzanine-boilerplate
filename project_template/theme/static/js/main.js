// Main site scripts

import '../scss/main.scss';

// Replace the 'no-js' class with the 'js' class on <html>
// Useful when CSS needs to determine if JS is enabled
document.documentElement.classList.remove('no-js');
document.documentElement.classList.add('js');
